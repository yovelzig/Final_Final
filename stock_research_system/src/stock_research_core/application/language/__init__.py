"""Shared English/Hebrew language bridge (spec G2D2 section 6): bounded
Unicode-range detection, a narrow `TranslationPort`, localized
learner-safe message pairs, and `LanguageBridgeService` - used by the
grounded AI tutor and (in a later phase) Live Research request
construction. Import submodules directly, e.g. `from
stock_research_core.application.language.detection import Language`.
"""
