"""Infrastructure-only pandas/NumPy implementation of the scenario
calculator. Never imported by `domain.market_scenarios` or
`application.market_scenarios` - only by the composition root (the
CLI/seed script)."""
