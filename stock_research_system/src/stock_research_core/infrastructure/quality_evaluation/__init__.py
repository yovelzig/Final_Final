"""Infrastructure adapters for the Phase 13 quality-evaluation platform.

RAGAS is imported only inside `ragas_adapter.py` and
`ragas_model_factory.py` - no other module in this package (or anywhere
outside it) imports `ragas` directly.
"""
